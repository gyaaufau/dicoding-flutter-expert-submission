workflows:
  android_ci:
    name: Ditonton Android CI
    max_build_duration: 60
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: main
          include: true
          source: true
    environment:
      flutter: stable
      xcode: latest
      java: 17
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - ~/.gradle/caches
        - ~/.gradle/wrapper
    scripts:
      - name: Setup Melos
        script: |
          REPO_ROOT="$(git rev-parse --show-toplevel)"
          cd "$REPO_ROOT"
          flutter pub get
          dart run melos bootstrap
      - name: Analyze Workspace
        script: |
          REPO_ROOT="$(git rev-parse --show-toplevel)"
          cd "$REPO_ROOT"
          flutter analyze
      - name: Run Workspace Tests
        script: |
          REPO_ROOT="$(git rev-parse --show-toplevel)"
          cd "$REPO_ROOT"
          dart run melos run test
      - name: Collect Coverage
        script: |
          REPO_ROOT="$(git rev-parse --show-toplevel)"
          cd "$REPO_ROOT"
          dart run melos coverage
      - name: Build Android APK
        script: |
          REPO_ROOT="$(git rev-parse --show-toplevel)"
          cd "$REPO_ROOT"
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      - coverage/lcov.info
    publishing:
      email:
        recipients:
          - argyaauliaf@gmail.com
        notify:
          success: true
          failure: true
